[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "endstone_chatty"
version = "0.0.1"
description = "Чатовый плагин"

[project.entry-points."endstone"]
chatty = "endstone_chatty:Chatty"